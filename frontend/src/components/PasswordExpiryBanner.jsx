import { useNavigate, useLocation } from 'react-router-dom';

/*export default function PasswordExpiryBanner({
  isExpired,
  showWarning,
  daysLeft,
}) {
  if (isExpired) {
    return (
      <div className="bg-red-600 text-white text-sm px-4 py-2 text-center">
        Your password has expired. Please reset it to continue using IntelliDGA.
      </div>
    );
  }
  if (showWarning && daysLeft > 0) {
    return (
      <div className="bg-amber-500 text-black text-sm px-4 py-2 text-center">
        Your password will expire in <b>{daysLeft}</b> day
        {daysLeft === 1 ? '' : 's'}. Please reset it to avoid lockout.
      </div>
    );
  }
  return null;
}*/
